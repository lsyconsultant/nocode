<mvc:View
        controllerName="sap.ui.eland.controller.campaign.campaignList"
        xmlns:mvc="sap.ui.core.mvc"
        xmlns="sap.m"
        xmlns:core="sap.ui.core"
        xmlns:form="sap.ui.layout.form"
        xmlns:u="sap.ui.unified"
        xmlns:tnt="sap.tnt"
        xmlns:t="sap.ui.table" height="100%" class="mainStyle">

    <Page id="campaignList" showHeader="false">
        <Panel width="auto" class="sapUiResponsiveMargin" accessibleRole="Region">
            <headerToolbar>
                <OverflowToolbar>
                    <Title text="조회 조건??"/>
                    <ToolbarSpacer/>
                    <Button icon="sap-icon://search" text="검색" type="Reject" press="getCompanyList"/>
                </OverflowToolbar>
            </headerToolbar>
            <content>
                <form:SimpleForm id="" editable="true" layout="ResponsiveGridLayout" labelSpanXL="4"
                                 labelSpanL="4" labelSpanM="12" labelSpanS="12" adjustLabelSpan="false"
                                 emptySpanXL="0" emptySpanL="0" emptySpanM="0" emptySpanS="0" columnsXL="2"
                                 columnsL="2" columnsM="1" singleContainerFullSize="false">
                    <form:content>
                        <Label text="ID"/>
                        <Input value=""/>
                        <Label text="기간"/>
                        <DateRangeSelection width="300px" delimiter="~" dateValue="{/respn_start_date}"
                                            secondDateValue="{/respn_end_date}" displayFormat="yyyy-MM-dd"
                                            valueFormat="yyyyMMdd"/>
                        <Label text="캠페인명"/>
                        <Input value=""/>
                        <Label text="유형"/>
                        <ComboBox items="{/campaignTypeDDLB}" selectedKey="test">
                            <core:Item key="{key}" text="{text}"/>
                        </ComboBox>
                        <Label text="생성자"/>
                        <Input value=""/>
                    </form:content>
                </form:SimpleForm>
            </content>
        </Panel>
        <Panel width="auto" class="sapUiResponsiveMargin" accessibleRole="Region">
            <headerToolbar>
                <OverflowToolbar>
                    <Title text="결과(5건)"/>
                </OverflowToolbar>
            </headerToolbar>
            <content>
                <t:Table class="Table1" rows="{/camlist}" selectionMode="Single" selectionBehavior="Row"
                         rowSelectionChange="onSegmentClick" visibleRowCount="20">
                    <t:columns>
                        <t:Column>
                            <Label text="캠페인"/>
                            <t:template>
                                <Link text="{name}" press="onCampaignClick"/>
                            </t:template>
                        </t:Column>
                        <t:Column>
                            <Label text="상태"/>
                            <t:template>
                                <Text text="{status}"/>
                            </t:template>
                        </t:Column>
                        <t:Column>
                            <Label text="유형"/>
                            <t:template>
                                <Text text="{type}"/>
                            </t:template>
                        </t:Column>
                        <t:Column>
                            <Label text="발송일정"/>
                            <t:template>
                                <Text text="{send}"/>
                            </t:template>
                        </t:Column>
                        <t:Column>
                            <Label text="세그먼트"/>
                            <t:template>
                                <Text text="{segment}"/>
                            </t:template>
                        </t:Column>
                    </t:columns>
                </t:Table>
            </content>
        </Panel>

    </Page>


</mvc:View>